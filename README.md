# Vue project ToDo List

## 🛠 Стек технологій

- **Vue 3** — прогресивний JavaScript фреймворк
- **TypeScript** — типізований JavaScript
- **Vite** — швидкий збірник і dev-сервер
- **Pinia** — сучасний стейт-менеджер для Vue
- **Tailwind CSS** — utility-first CSS фреймворк
- **ESLint** — лінтер коду
- **pnpm** — швидкий пакетний менеджер

## 📜 Скрипти

```bash
pnpm dev      # Запуск у режимі розробки (з hot reload)
pnpm build    # Білд застосунку для продакшену
pnpm preview  # Попередній перегляд збірки
pnpm lint     # Перевірка коду через ESLint
```

## 📁 Структура проєкту

```
src/
├── assets/           # Статичні файли (зображення, іконки)
├── components/       # Vue компоненти
│   └── common/       # Переважні компоненти
├── stores/           # Pinia стейт-менеджмент
├── views/            # Сторінки застосунку
│   └── Home.vue      # Головна сторінка
├── router/           # Vue Router конфігурація
├── types/            # TypeScript типи
├── utils/            # Допоміжні функції
├── main.ts           # Точка входу
└── main.scss         # Глобальні стилі
```

