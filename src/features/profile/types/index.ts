export interface ProfileResponse {
  email: string;
  name: string;
  id: string;
  role: string;
  createdAt: string;
  updatedAt: string;
}

export interface Profile {
  email: string;
  name: string;
  role: string;
  createdAt: string;
}

